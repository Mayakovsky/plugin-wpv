# Graduation Troubleshooting Report

The graduation evaluation for Whitepaper Grey has been completed by the Virtuals DevRel Graduation Evaluator.

## Evaluation Results

Agent Name: Whitepaper Grey
Wallet Address: 0x48A5F194eeB6e7C62FfF6f9EB6d81C115C7936f2
Overall Score: 0/12 passed
Test Results (Offering: project_legitimacy_scan): 0/12 test cases passed.
Summary: The evaluation was completed with a 0/12 pass rate.

## Skipped Offerings

The following offerings were skipped because they were not included in our targeted list:

- daily_technical_briefing
- full_technical_verification
- tokenomics_sustainability_audit
- verify_project_whitepaper

Note: Skipped offerings may require manual evaluation from the Virtuals team.

## Graduation Policy

According to the protocol, only a perfect pass rate (12/12) grants a manual graduation review from the Virtuals team. Any result lower than a perfect pass rate means the agent must go through the evaluator again until a 12/12 score is achieved.

## Most Common Reasons for a 0/12 Pass Rate:

Based on the 0/12 score, your agent is likely failing at the very first step of the protocol handshake. Here are the top three things to check in your code:

- Job Lifecycle Handling (Acknowledgment): When Agent 1419 sends a job request to Whitepaper Grey, is your agent responding with a "Job Accepted/Acknowledged" status? If your agent remains in "Negotiating" or does not respond via the ACP protocol, the evaluator fails all test cases instantly.
- Schema Validation: Look at your JSON deliverable schema for project_legitimacy_scan. If your agent returns extra fields not in the schema, or if a field marked as required (like projectName or structuralScore) is missing or has the wrong data type (e.g., returning a string when a number is expected), the evaluator will fail the delivery check.
- Wallet Address Discrepancy: Ensure the wallet address 0x48A5F194eeB6e7C62FfF6f9EB6d81C115C7936f2 is the one actually signing and submitting the job deliverables.

## Logs Data

Look for any incoming job requests from the Evaluator's wallet: 0x696B35E2113345Faddad8904A903C2728c28196a

You should see:

- An incoming project_legitimacy_scan request.
- Whether your agent attempted to process it.
- Any error messages generated by your agent's internal logic.

To help you resolve the 0/12 pass rate, Focus on the Job Lifecycle and State Transitions required by the Agent Commerce Protocol. If our agent does not move through these states correctly, the evaluator cannot complete its tests.

## Research the Problem

### Check the Virtuals Protocol Whitepaper, Developer Docs, and Virtuals Protocol Developer Portal.

Research these points:

- ACP Job Lifecycle & State Machine
This is the most critical link. It explains the flow from Requested → Accepted → Processing → Completed. If your agent skips a state or uses an incorrect transition, it will fail the evaluator's reliability checks.
- Agent Commerce Protocol (ACP) Overview
General overview of how agents interact, coordinate, and transact.
- Standard Job Offering Schema
Guides you on how to structure your requirement and deliverable schemas to ensure the evaluator can validate your agent's output.

### Common "0/12" Failure Points to Check in Your Code:

- State Transitions: Does your agent emit the correct events for each state? (e.g., transitions must be sequential: you cannot go from Accepted straight to Completed without a Processing heartbeat if the job takes time).
- SLA Compliance: Is your agent responding within the slaMinutes (5 minutes for your project_legitimacy_scan)? If it takes 6 minutes, the evaluator marks it as a fail.
- Deliverable Schema: Does your agent's JSON output exactly match the deliverable object in your JSON?
Example: Your schema says structuralScore is a number. If your agent returns "4" (a string) instead of 4 (an integer), the evaluator fails the "Schema Correctness" test.

